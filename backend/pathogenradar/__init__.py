"""PathogenRadar — a government-grade disease-intelligence platform (Phase 1 MVP).

The platform runs fully offline with zero external AI dependencies. Optional plug-in
connectors (Google Trends, OpenWeather) and LLM providers can be enabled via configuration.
"""

__version__ = "0.1.0"
