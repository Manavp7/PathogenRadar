"""Reinforcement-learning alert optimization (Phase 2).

A tabular Q-learning agent learns *when* to raise an outbreak alert to maximise early
detection while minimising false alarms — tuning the alert threshold beyond the fixed rule.
Pure NumPy; no torch.
"""
